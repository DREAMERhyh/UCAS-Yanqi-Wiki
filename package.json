{
  "scripts": {
    "pregenerate": "node scripts/generate-reviews.js",
    "predocs:build": "node scripts/generate-reviews.js",
    "generate:reviews": "node scripts/generate-reviews.js",
    "docs:dev": "npm exec -- vitepress dev docs",
    "docs:build": "npm exec -- vitepress build docs",
    "docs:preview": "npm exec -- vitepress preview docs",
    "build": "npm run docs:build"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "vitepress": "^1.6.4",
    "vue": "^3.5.34"
  }
}
